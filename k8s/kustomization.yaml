apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

images:
  - name: jiang0613/gitops-demo
    newTag: sha-REPLACEME  # 这里会被 GitHub Actions 自动替换